[project]
name = "no-read"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.8.0,<4.0.0",
    "aiolimiter>=1.0.0",
    "httpx>=0.28.1",
]
